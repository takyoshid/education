# 対応バージョン

この教材が想定するツールの版を、**ここ 1 枚にまとめてあります。**

版を上げるときは、この表を書き換えて `python3 scripts/validate_versions.py` を実行してください。表と実際の設定ファイルがずれていれば、そこで落ちます。

## 表

| ツール | 版 | 主な使用箇所 |
|---|---|---|
| Python | 3.12 以上 | Phase 2 以降のすべて、教材同梱の API サーバ |
| Node.js | 22 以上 | Phase 6 |
| PostgreSQL | 16 | Phase 7、Phase 10 |
| React | 19 | Phase 6、Phase 12 |
| TypeScript | 5.7 | Phase 6、Phase 12 |
| Vite | 6 | Phase 6 |
| Ruby | 3.3 | 教材の検査スクリプトのみ(学習者には不要) |

CI は Python 3.12 と 3.13 の両方で検査します。**学習者が今インストールする版は最新であることが多い**ので、最小版だけでなく新しい版でも動くことを確かめるためです。

## なぜ 1 枚にまとめるのか

版の数字は、この教材で**最も早く腐る情報**です。

これがレッスン本文のあちこちに散っていると、版を 1 つ上げるだけで何十ものファイルを触ることになります。触る回数が増えれば、そのたびに「100 年もつはずの説明文」にも編集の手が入り、少しずつ削れていきます。

数字を 1 箇所に集めておけば、**更新のたびに触るファイルは 1 つ**で済みます。腐る部分と腐らない部分を、物理的に分けるということです。

## ただし、すべての版数をここに集めるわけではありません

**これは重要な区別です。** 版の数字には、性質のまったく違う 2 種類があります。

### 1. 環境要件 — ここに集める(腐る)

「動かすために何が要るか」を述べたもの。時間が経てば変わります。

- 「Node.js 22 以上が必要」
- `package.json` の `"react": "^19.0.0"`
- CI の `python-version: ["3.12", "3.13"]`

これらは**この表が唯一の情報源**です。各 Phase の README やプロジェクトの設定ファイルがこの表と食い違っていたら、CI が失敗します。

### 2. 歴史的事実 — レッスン本文に残す(腐らない)

「いつ何が入ったか」を述べたもの。**これは永久に変わりません。**

- 「`dict` が挿入順を保つのは Python 3.7 以降」
- 「型ヒントは Python 3.5 で導入された」
- 「`int | None` と書けるのは Python 3.10 以降」
- 「Python 2 と Python 3 では `/` の挙動が違う」

Python 3.7 が `dict` の順序を保証した年は、100 年後も 2018 年のままです。**これらを「古いから」といって書き換えてはいけません。**書き換えると、なぜその書き方が存在するのかという理由が消えます。

言語の機能には必ず「入った時点」があり、その前後で書き方が違います。その事情を知っていることが、古いコードを読める力になります。

### 見分け方

迷ったときは、こう問いてください。

> **10 年後、この文は間違いになるか？**

- 「Python 3.12 以上が必要」→ 10 年後には間違い。**表に集める**
- 「型ヒントは Python 3.5 で導入された」→ 10 年後も正しい。**本文に残す**

## 検査されている場所

`scripts/validate_versions.py` は、表と次の場所が一致していることを確かめます。

| 場所 | 何を検査するか |
|---|---|
| `.github/workflows/curriculum-quality.yml` | `python-version` / `node-version` / `ruby-version` |
| `phase6-web-frontend/project/stage2-react/package.json` | react / typescript / vite |
| `phase6-web-frontend/exercises/solutions/ex06-react-solution/package.json` | react / typescript / vite |
| 各 Phase README の「前提知識」節 | 要求している版 |
| `phase12-projects-oss/capstone/` の技術スタック | 指定している版 |
| `longitudinal-project/README.md` | 要求している版 |
| Docker イメージのタグ(`postgres:16-alpine` など) | イメージの版 |

レッスン本文はこの検査の対象外です。上に書いたとおり、そこに出てくる版数は歴史的事実であり、表に合わせる対象ではないからです。

## 依存パッケージの固定について

版の表とは別に、**フロントエンドの依存は `package-lock.json` で固定してあります。**

固定と、固定しない検査の両方が要ります。目的が違うからです。

| 入れ方 | 何を守るか | 落ちたときの意味 |
|---|---|---|
| `npm ci`(ロックのとおり) | 再現性。学習者の手元と、検証した木が同じになる | 固定した木がもう入らない。**すぐ直す** |
| `npm install --no-package-lock`(解決し直す) | 腐りの早期発見 | 依存側の変更で教材が腐り始めた。**学習者に届く前に気づけた** |

CI はこの 2 通りを両方実行します。Python 側で複数のバージョンを検査しているのと同じ考え方です(理由は [`phase10-infra-cloud/project/requirements.txt`](phase10-infra-cloud/project/requirements.txt) の冒頭に書いてあります)。

**学習者は `npm install` を使ってください。** ロックがあればそのとおりに入り、将来ロックが使えなくなっても解決し直して先に進めます。厳しく落ちるのは CI の側だけでよく、学習者の手を止める必要はありません。

なお `phase6-web-frontend/assessment/starter/` にロックはありません。**依存がゼロだからです。**これは意図的な設計で、150 分の実技試験中にパッケージのインストールを発生させないためのものです。

### 依存を変えたとき

```bash
cd phase6-web-frontend/project/stage2-react
npm install          # package.json を編集したあとに実行するとロックが更新される
npm ci               # ロックのとおりに入り直せることを確認する
npm run build
```

`package.json` だけを編集してロックを更新し忘れると、**CI の `npm ci` が失敗します。**これは意図した動作です。

## 版を上げる手順

1. この表を書き換える
2. `python3 scripts/validate_versions.py` を実行する
3. 落ちた箇所を、表に合わせて直す
4. テストを実行する(`README.md` の「教材自体の品質確認」を参照)

検査が落ちた箇所こそが、「版を上げると腐る場所」の全体像です。数が増えてきたら、それは特定の製品に教材が寄りかかりすぎている合図だと考えてください。
