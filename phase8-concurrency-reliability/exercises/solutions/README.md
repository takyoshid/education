# Phase 8 参照実装

各ディレクトリには、対応する公開テストを満たす実装例があります。これは唯一の正解ではありません。先に starter を自力で実装し、テストを通して設計理由を説明した後で、比較と振り返りに使用してください。

参照実装は starter の公開テストを複製していません。CI は同じテスト一式を一時ディレクトリへコピーし、starter では意図どおり失敗し、参照実装ではすべて成功することを別々に確認します。
